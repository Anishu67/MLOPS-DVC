CREATE A GIT REPPO AND CLONE IT IN LOCAL
create a mycode.py and code to it. (it will save a csv file to a new "data" folder)
do a git add commit push before initislizing dvc
pip install dvc
now do dvc init (create,.dvcignore), .dvc
now do mkdir S3 (creates new s3 directory)
Now we do dvc remote add -d myremote s3
next "dvc add data/'
now it will ask you to do ("git rm -r --cached 'data' and "git commit -m"stop tracking data")
because initially we are tracking data from git and now the dvc will handle the data
again we do dvc add data/ (creates data.dvc) then git add .gitignore data.dvc\
now "dvc commit" amd then dvc push

do a git add-commit-push to mark this stage as first version of datanow make changes to mycode.py to append new row in data check changes via dvc status

